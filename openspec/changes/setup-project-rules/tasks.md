## 1. OpenSpec and Project configuration

- [x] 1.1 Update `openspec/config.yaml` to configure the project-level context for Next.js 14 App Router, standard Tailwind CSS, no i18n, simple layouts, and soft pastel/light colors
- [x] 1.2 Update the rules in `openspec/config.yaml` to ensure OpenSpec proposals, designs, and tasks validation enforce the minimalist aesthetic and direct text rule

## 2. AI Rules and Legacy Cleanup

- [x] 2.1 Create a `.cursorrules` file at the root of the workspace to enforce coding and design constraints directly on AI assistants like Antigravity
- [x] 2.2 Refactor the root-level `project.rules.md` to remove any legacy multilingual (English/French) references and adjust portfolio requirements to match a simple, soft-colored style
- [x] 2.3 Verify the configuration is correct by running a dry compile and inspecting both `.cursorrules` and `project.rules.md`
